// Ajouter dans tailwind.config.js > theme > extend > colors
// Palette issue du logo FDF

colors: {
    brand: {
        blue:    '#16348C',   // bleu royal — couleur principale
        'blue-dk': '#0E2566', // bleu foncé — textes/fonds intenses
        gold:    '#D9A521',   // or doré — boutons CTA, accents
        'gold-lt': '#F1CE6E', // or clair — survols, dégradés
        red:     '#E12330',   // rouge cœur — accent ponctuel
    },
    ink: {
        dark: '#1E2533',      // texte principal
        grey: '#59606E',      // texte secondaire
    },
    paper: {
        blue: '#EEF2FB',      // fond bleuté (sections alternées)
        gold: '#FBF4E2',      // fond doré (callouts)
    },
},
